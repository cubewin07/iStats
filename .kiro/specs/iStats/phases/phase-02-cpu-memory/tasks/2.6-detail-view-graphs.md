# Task 2.6 — Render CPU + memory in the detail view

**Phase:** 2 — CPU & Memory
**Requirements:** 9.4, 10.1, 10.2, 10.3

## What to do

- Render CPU (aggregate + per-core) and memory in the detail view with live rolling graphs
  fed from `MetricsStore`.
- Allow choosing CPU or memory to appear in the menu bar.

## Goal (definition of done)

- Detail view shows current values + rolling history graphs and refreshes live
  (10.1–10.3).
- The user can select CPU or memory for the menu bar display (9.4).
