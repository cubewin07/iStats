# Task 0.3 — Write the core documentation set

**Phase:** 0 — Documentation scaffolding & learning baseline
**Requirements:** 14.2

## What to do

Write the general documentation set:

- `docs/overview.md` — what iStats is, who it's for, scope and non-goals.
- `docs/architecture.md` — the layered design (Sampling → Core → UI) and threading model.
- `docs/build-and-run.md` — how to build, run, and debug the app locally.
- `docs/glossary.md` — SMC, IOKit, sysctl, Mach, sampler, sample, menu bar extra.

## Goal (definition of done)

- All four documents exist and are internally consistent with the top-level
  [`design.md`](../../design.md).
- A new contributor can build/run the app and understand the architecture from these docs
  alone.
