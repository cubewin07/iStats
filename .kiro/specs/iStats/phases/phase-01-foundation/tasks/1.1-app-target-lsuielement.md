# Task 1.1 — Create the Swift app target with LSUIElement

**Phase:** 1 — Foundation
**Requirements:** 9.1, 9.5

## What to do

- Create the Swift app target for iStats.
- Set `LSUIElement` so the app runs as a menu bar app without a Dock icon (keep it
  configurable for a later Dock-icon toggle).

## Goal (definition of done)

- The app builds and launches with no Dock icon.
- The Dock-icon behavior is driven by a setting that Phase 6 can flip.
