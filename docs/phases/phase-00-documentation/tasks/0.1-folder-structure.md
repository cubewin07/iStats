# Task 0.1 — Create the project + documentation folder structure

**Phase:** 0 — Documentation scaffolding & learning baseline
**Requirements:** 14.2, 14.3

## What to do

Create the `iStats/` folder structure exactly as defined in the design:

- `iStats/App`, `iStats/Core`, `iStats/Sampling`, `iStats/UI`, `iStats/Resources`
- `iStatsTests/`
- `docs/` with the `adr/` and `phases/` subfolders

## Goal (definition of done)

- All folders exist and match the skeleton in
  [`design.md`](../design.md) / the top-level design.
- Empty or stub placeholder files are acceptable where content arrives in later tasks.
- The layout makes the separation of Sampling / Core / UI obvious at a glance.
