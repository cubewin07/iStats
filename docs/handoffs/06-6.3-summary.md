# Summary — 6.3 Persist All Preferences Across Launches

## Done
- Validated complete round-trip persistence and recovery for all user preferences in `PreferencesStore`:
  - `refreshInterval`: Double (clamped to `[0.5, 60.0]`).
  - `enabledCategories`: Set of `MetricCategory` (stored as string raw values).
  - `temperatureUnit`: `Units.TemperatureUnit` (`.celsius`, `.fahrenheit`).
  - `networkUnit`: `Units.NetworkUnit` (`.bytesPerSecond`, `.bitsPerSecond`).
  - `byteUnitStandard`: `Units.ByteUnitStandard` (`.iec`, `.si`).
  - `showDockIcon`: Bool (synced with activation policy).
  - `launchAtLogin`: Bool (synced with `SMAppService`).
  - `menuBarDisplayMode`: `MenuBarDisplayMode` (`.icon`, `.cpu`, `.memory`, `.both`, `.network`, `.battery`, `.thermal`, `.gpu`).
- Enforced ADR 0006 compliance (Telemetry Privacy & Zero Persistence):
  - Verified that strictly zero telemetry readings, ring buffer samples, or system metrics are ever written to `UserDefaults` or disk.
- Added comprehensive unit test suite in `Tests/iStatsCoreTests/PreferencesPersistenceTests.swift`:
  - `testFullRoundTripPersistenceAcrossLaunches`: Complete multi-instance reload testing across isolated `UserDefaults` suites.
  - `testClampingOnDeserialization`: Out-of-bounds interval clamping tests.
  - `testCorruptedDataGracefulFallback`: Graceful recovery from corrupt/invalid raw values.
  - `testADR0006ZeroTelemetryPersisted`: Strict whitelist assertion that only defined configuration keys exist in storage.
  - `testResetToDefaultsPersists`: Verified defaults restoration across app reboots.
- Ran all tests:
  - `swift test --scratch-path /tmp/istats-build` — **120 passed** (0 failures).
  - `xcodebuild test -scheme iStatsApp -destination 'platform=macOS'` — **151 passed** (0 failures).

## Files changed
- `Tests/iStatsCoreTests/PreferencesPersistenceTests.swift` — **[NEW]** Persistence and ADR 0006 validation test suite.
- `docs/handoffs/06-6.3-summary.md` — **[NEW]** Handoff summary for Task 6.3.
- `docs/progress.md` — Updated Task 6.3 to done, Next task set to `6.4`.

## Tests run
- `swift test --scratch-path /tmp/istats-build` — **120 passed** (0 failures)
- `xcodebuild test -scheme iStatsApp -destination 'platform=macOS'` — **151 passed** (0 failures)
