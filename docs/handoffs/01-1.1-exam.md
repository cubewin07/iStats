# Exam — 1.1 Create the Swift App Target with LSUIElement

## Snapshot
- `Package.swift` defines `iStatsCore` library and `iStatsCoreTests` (19 passing unit tests).
- `Sources/iStatsCore/` contains pure types, models, math, and ring buffer.
- No Xcode project (`iStats.xcodeproj`) or app target exists on disk yet.
- No `Info.plist` or `LSUIElement` configuration exists yet.

## Spec coverage
| Requirement / task bullet | Status | Evidence |
|---------------------------|--------|----------|
| 9.1 Menu bar status item install | Pending (Task 1.2) | App target skeleton needed first |
| 9.5 Run without Dock icon (configurable) | Implement now | `LSUIElement = true` in Info.plist + `DockIconManager` |
| Create Swift app target | Implement now | `iStats.xcodeproj` linking `iStatsCore` |

## Relevant types and files
- `Package.swift` — Pure core library package
- `iStats/App/` — App target entry point, delegate, and Dock manager
- `iStats/Resources/Info.plist` — Application configuration & LSUIElement
- `iStats.xcodeproj` — Xcode project with shared scheme

## Risks and unknowns
- Xcode project format: needs to correctly reference local `iStatsCore` SPM package and build cleanly under Swift 6.0 and macOS 13+.
- App activation policy: `NSApp.setActivationPolicy` must be safely callable on `@MainActor`.

## Suggested next role
implementor

## Open questions
None.
