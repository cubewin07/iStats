# Plan — 3.2 Network rate math with counter-reset handling

**Role:** Planner
**Phase:** 3 — Network & Disk
**Task:** 3.2 Network rate math with counter-reset handling
**Requirements:** 6.4

---

## 1. Context & Objectives

- Requirement 6.4: Derive throughput from byte deltas between consecutive samples and handle counter resets without producing negative or absurd values.
- Property test the rate function `RateMath.bytesPerSecond` and `RateMath.counterDelta` across edge cases, extreme bounds, negative/zero elapsed time, and randomized fuzz vectors.
- Property test multi-sample session totals in `NetworkSampler` ensuring monotonic growth and stability under frequent interface restarts and counter wraps.

---

## 2. Implementation Steps

1. **Expand `RateMathTests.swift`** (`Tests/iStatsCoreTests/RateMathTests.swift`):
   - Add property tests for non-negativity across full domain.
   - Add property tests for counter reset invariance (`current < previous` -> rate 0).
   - Add property tests for scale invariance and time partition additivity.
   - Add monotonicity property tests (monotonic non-decreasing with respect to counter increase, monotonic decreasing with respect to elapsed time).
   - Add comprehensive fuzz testing across extreme 32-bit and 64-bit boundaries (0, 2^32 - 1, 2^32, 2^32 + 1, 2^64 - 1, etc.).

2. **Expand `NetworkSamplerTests.swift`** (`Tests/iStatsTests/NetworkSamplerTests.swift`):
   - Add multi-cycle property test: simulate 100 random sampling cycles with periodic counter resets and verify session totals are strictly monotonic non-decreasing while instantaneous rates remain well-formed (never negative, 0 on reset).
   - Test extreme elapsed time steps (zero elapsed time, negative time step from clock skew).

3. **Verification & Handoff**:
   - Run `swift test --scratch-path /tmp/istats-build`.
   - Run `xcodebuild test -scheme iStatsApp -destination 'platform=macOS'`.
   - Update `docs/progress.md` and write `docs/handoffs/03-3.2-summary.md`.
   - Commit Task 3.2.
