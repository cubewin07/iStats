# Summary — 6.2 Launch at Login + Dock-Icon Toggle

## Done
- Implemented `LaunchAtLoginManager` using modern macOS 13+ `ServiceManagement.SMAppService.mainApp`:
  - Provides reactive `status` (`SMAppService.Status`) and `isRegistered` state.
  - Safe, non-throwing registration/unregistration API (`setLaunchAtLogin(enabled:)`).
  - Graceful degradation and logging when executed in sandboxed/unbundled/CLI test environments.
- Updated `AppDelegate.swift`:
  - Enforces `DockIconManager.shared.applyCurrentPolicy()` upon application launch.
  - Synchronizes `LaunchAtLoginManager.shared.setLaunchAtLogin(enabled: true)` if user preference `PreferencesStore.shared.launchAtLogin` is enabled.
- Updated `PreferencesView.swift`:
  - Connected `Toggle("Launch at Login", isOn: $store.launchAtLogin)` with `.onChange` to invoke `LaunchAtLoginManager.shared.setLaunchAtLogin(enabled:)`.
  - Connected "Reset to Defaults" button to restore both `DockIconManager` and `LaunchAtLoginManager` to default off state.
- Added comprehensive unit and integration tests in `Tests/iStatsTests/SystemIntegrationTests.swift`:
  - Verified `DockIconManager` defaults, toggling, and policy application.
  - Verified `LaunchAtLoginManager` status querying and graceful degradation.
  - Live registration and unregistration verified on host macOS.
  - Verified `PreferencesStore` synchronization for launch at login.
- Added `LaunchAtLoginManager.swift` and `SystemIntegrationTests.swift` to Xcode project `iStats.xcodeproj/project.pbxproj`.
- Ran all tests:
  - `swift test --scratch-path /tmp/istats-build` — **115 passed** (0 failures).
  - `xcodebuild test -scheme iStatsApp -destination 'platform=macOS'` — **151 passed** (0 failures).

## Files changed
- `iStats/App/LaunchAtLoginManager.swift` — **[NEW]** Native login item management via `SMAppService`.
- `iStats/App/AppDelegate.swift` — Added login item synchronization on app startup.
- `iStats/UI/PreferencesView.swift` — Wired login item toggle and reset handlers.
- `Tests/iStatsTests/SystemIntegrationTests.swift` — **[NEW]** Unit & integration tests for system services.
- `iStats.xcodeproj/project.pbxproj` — Added new source and test files.
- `docs/handoffs/06-6.2-summary.md` — **[NEW]** Handoff summary for Task 6.2.
- `docs/progress.md` — Updated Task 6.2 to done, Next task set to `6.3`.

## Tests run
- `swift test --scratch-path /tmp/istats-build` — **115 passed** (0 failures)
- `xcodebuild test -scheme iStatsApp -destination 'platform=macOS'` — **151 passed** (0 failures)
