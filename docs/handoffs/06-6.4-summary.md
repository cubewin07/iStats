# Summary — 6.4 Performance Pass

## Done
- Validated off-main-thread execution invariant (Requirement 12.1, ADR 0002):
  - Verified across all 8 samplers (`CPUSampler`, `MemorySampler`, `NetworkSampler`, `DiskSampler`, `PowerSampler`, `ThermalSampler`, `FanSampler`, `GPUSampler`) that sampling tasks run exclusively on scheduler background tasks (`Thread.isMainThread == false`).
- Measured sampling latency & host CPU footprint (Requirement 12.2):
  - Benchmarked 10 consecutive sampling passes across all 8 hardware samplers combined on host hardware.
  - Average total pass duration is ~4–8 ms, representing an idle CPU duty cycle of << 0.5% at the default 2.0s interval.
- Verified dynamic interval scaling (Requirement 12.4):
  - Verified that increasing the refresh interval from 0.2s to 2.0s proportionally reduces background invocation frequency.
- Verified zero overhead for disabled categories (Requirement 12.2):
  - Confirmed that disabled metric categories have their periodic background tasks cancelled immediately, generating 0 sampling passes and 0 IOKit/Mach/sysctl calls.
- Added comprehensive performance test suite `Tests/iStatsTests/PerformancePassTests.swift`:
  - `testAllSamplersExecuteOffMainThread`
  - `testFullSamplingPassLatencyBenchmark`
  - `testIntervalScalingReducesSamplingFrequency`
  - `testDisabledCategoriesIncurZeroSamplingOverhead`
- Added `PerformancePassTests.swift` to Xcode project `iStats.xcodeproj/project.pbxproj`.
- Ran all tests:
  - `swift test --scratch-path /tmp/istats-build` — **120 passed** (0 failures).
  - `xcodebuild test -scheme iStatsApp -destination 'platform=macOS'` — **155 passed** (0 failures).

## Files changed
- `Sources/iStatsCore/SampleScheduler.swift` — Added `setOnSample`, `setOnMainActorSample`, and expanded initializer.
- `Tests/iStatsTests/PerformancePassTests.swift` — **[NEW]** Off-main-thread, latency benchmark, and interval scaling test suite.
- `iStats.xcodeproj/project.pbxproj` — Registered `PerformancePassTests.swift`.
- `docs/handoffs/06-6.4-summary.md` — **[NEW]** Handoff summary for Task 6.4.
- `docs/progress.md` — Updated Task 6.4 to done, Next task set to `6.5`.

## Tests run
- `swift test --scratch-path /tmp/istats-build` — **120 passed** (0 failures)
- `xcodebuild test -scheme iStatsApp -destination 'platform=macOS'` — **155 passed** (0 failures)
