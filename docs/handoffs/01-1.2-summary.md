# Summary — 1.2 Install the status item and detail popover

## Done
- Created `iStats/UI/DetailPopoverView.swift`: SwiftUI view displaying the detail popover shell (header, system monitor placeholder, and quick controls).
- Created `iStats/App/MenuBarController.swift`: `@MainActor` class managing the `NSStatusItem` in the system menu bar, configured with a template SF Symbol icon, and an `NSPopover` with `.transient` behavior hosting `DetailPopoverView` via `NSHostingController`.
- Updated `iStats/App/AppDelegate.swift`: Retains and instantiates `menuBarController` during `applicationDidFinishLaunching(_:)`.
- Updated `iStats.xcodeproj/project.pbxproj`: Configured `UI` group and added `MenuBarController.swift` and `DetailPopoverView.swift` to the `iStats` application build target.
- Built and verified application compilation via `xcodebuild -scheme iStats -configuration Debug clean build`.
- Ran full test suite via `swift test --scratch-path /tmp/istats-build` (19 passed).

## Files changed
- `iStats/UI/DetailPopoverView.swift` — SwiftUI popover content view shell
- `iStats/App/MenuBarController.swift` — AppKit menu bar status item and popover lifecycle controller
- `iStats/App/AppDelegate.swift` — Instantiates and retains `MenuBarController` on launch
- `iStats.xcodeproj/project.pbxproj` — PBX build files, file references, and groups
- `docs/handoffs/01-1.2-exam.md` — Examiner report
- `docs/handoffs/01-1.2-plan.md` — Implementation plan
- `docs/handoffs/01-1.2-summary.md` — Implementation summary

## Tests run
- `xcodebuild -scheme iStats -configuration Debug clean build` — **BUILD SUCCEEDED**
- `swift test --scratch-path /tmp/istats-build` — **19 passed**

## Not done / follow-ups
- Task 1.3: Define core protocols and value types (`Sample<T>`, `Sampler`, `Availability`, `MetricCategory`). Note: types exist in `iStatsCore`, next task will verify completeness against spec.
- Task 1.4: Implement `SampleScheduler`.

## Docs touched
- `docs/progress.md` — Marked 1.2 `done`, set Next task to `1.3`, updated On disk table.
