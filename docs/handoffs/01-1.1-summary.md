# Summary — 1.1 Create the Swift App Target with LSUIElement

## Done
- Created `iStats/Resources/Info.plist` with `LSUIElement` set to `true`, configuring the app as an accessory/agent without a Dock icon.
- Created `iStats/App/DockIconManager.swift` on `@MainActor` to manage switching `NSApplication.ActivationPolicy` between `.accessory` (hidden Dock icon) and `.regular` (visible Dock icon) backed by user preferences.
- Created `iStats/App/AppDelegate.swift` and `iStats/App/iStatsApp.swift` as the AppKit/SwiftUI entry point.
- Created `iStats.xcodeproj` (with `project.pbxproj` and `iStats.xcscheme`) configured for macOS 13+, Swift 6, non-sandboxed (ADR 0005), and linking the local `iStatsCore` package.
- Built the application with `xcodebuild -scheme iStats -configuration Debug build` (BUILD SUCCEEDED).
- Verified `LSUIElement` is `true` in the compiled application bundle via `plutil`.
- Ran full test suite via `swift test --scratch-path /tmp/istats-build` (19 passing).

## Files changed
- `iStats/Resources/Info.plist` — Application properties with `LSUIElement = true`
- `iStats/App/DockIconManager.swift` — Dynamic Dock icon activation policy manager
- `iStats/App/AppDelegate.swift` — `NSApplicationDelegate` lifecycle handling
- `iStats/App/iStatsApp.swift` — SwiftUI `@main` application entry point
- `iStats.xcodeproj/project.pbxproj` — Xcode project settings and package dependencies
- `iStats.xcodeproj/xcshareddata/xcschemes/iStats.xcscheme` — Shared scheme for Xcode and xcodebuild
- `docs/handoffs/01-1.1-exam.md` — Examiner report
- `docs/handoffs/01-1.1-plan.md` — Implementation plan
- `docs/handoffs/01-1.1-summary.md` — Implementation summary

## Tests run
- `xcodebuild -scheme iStats -configuration Debug build` — **BUILD SUCCEEDED**
- `plutil -p .../Debug/iStats.app/Contents/Info.plist` — Verified `"LSUIElement" => true`
- `swift test --scratch-path /tmp/istats-build` — **19 passed**

## Not done / follow-ups
- Task 1.2: Install status item (`NSStatusItem`) and detail popover.
- Task 1.3: Core protocols/types verification.
- Task 1.4: `SampleScheduler`.

## Docs touched
- `docs/progress.md` — Marked 1.1 `done`, set Next task to `1.2`, updated On disk table.
