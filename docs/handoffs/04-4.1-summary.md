# Summary — 4.1 PowerSampler: charge / state / time remaining

## Done
- Created `PowerSampler` in `iStats/Sampling/PowerSampler.swift`:
  - Defined `RawPowerSourceSnapshot` holding charge capacity, max capacity, state, charging/charged flags, time-to-empty/full, and time-remaining estimate.
  - Defined `PowerInfoProvider` protocol for dependency injection and testing.
  - Implemented `HostPowerInfoProvider` using `IOPowerSources` (`IOPSCopyPowerSourcesInfo`, `IOPSCopyPowerSourcesList`, `IOPSGetPowerSourceDescription`, and `IOPSGetTimeRemainingEstimate`).
  - Implemented `PowerSampler` conforming to `Sampler` (`Output = PowerSample`, `category = .power`).
  - Pure calculation in `calculateSample` mapping charge % (0...100), `BatteryState` (`.charging`, `.discharging`, `.charged`, `.acConnected`, `.unknown`), and time remaining in seconds (`TimeInterval`).
  - Handled the calculating/unknown time remaining case by returning `nil` (never zero or negative values).
- Updated `MetricsCoordinator.swift` in `iStats/App/`:
  - Added `@Published public private(set) var latestPower: Sample<PowerSample>?`.
  - Added `@Published public private(set) var powerHistory: [Sample<PowerSample>] = []`.
  - Registered `PowerSampler()` in `start()`.
  - Handled `.power` readings in `handleReading(_:)`.
- Updated Xcode Project (`iStats.xcodeproj/project.pbxproj`):
  - Registered `PowerSampler.swift` under `Sampling` group in `iStats` app target.
  - Registered `PowerSamplerTests.swift` under `Tests/iStatsTests` in `iStatsTests` test target.
- Added comprehensive unit tests in `Tests/iStatsTests/PowerSamplerTests.swift`:
  - `testPowerSamplerCategory` (.power).
  - `testPowerSamplerDischargingStateAndTimeRemaining`.
  - `testPowerSamplerChargingState`.
  - `testPowerSamplerChargedState`.
  - `testPowerSamplerACConnectedNotCharging`.
  - `testPowerSamplerTimeRemainingCalculatingReturnsNil`.
  - `testPowerSamplerThrowsWhenProviderFails`.
  - `testPowerSamplerNoBatteryMachine`.
- Verified test suites:
  - `swift test --scratch-path /tmp/istats-build` — 96 passed (0 failures).
  - `xcodebuild test -scheme iStatsApp -destination 'platform=macOS'` — 70 passed (0 failures).
  - `xcodebuild -scheme iStats -configuration Debug build` — BUILD SUCCEEDED.

## Files changed
- `iStats/Sampling/PowerSampler.swift` — `PowerSampler`, `RawPowerSourceSnapshot`, `RawSmartBatteryData`, `PowerInfoProvider`, `HostPowerInfoProvider`
- `iStats/App/MetricsCoordinator.swift` — Registered `PowerSampler` and published power state
- `Tests/iStatsTests/PowerSamplerTests.swift` — Unit tests for `PowerSampler`
- `iStats.xcodeproj/project.pbxproj` — Added `PowerSampler.swift` and `PowerSamplerTests.swift`
- `docs/handoffs/04-4.1-summary.md` — Handoff summary
- `docs/progress.md` — Updated status and on-disk tracking

## Tests run
- `swift test --scratch-path /tmp/istats-build` — **96 passed** (0 failures)
- `xcodebuild test -scheme iStatsApp -destination 'platform=macOS'` — **70 passed** (0 failures)
- `xcodebuild -scheme iStats -configuration Debug build` — **BUILD SUCCEEDED**

## Not done / follow-ups
- Task 4.2: Battery health metrics (cycle count, condition, design vs current capacity via `AppleSmartBattery`).
- Task 4.3: Instantaneous power draw / wattage and adapter power.

## Docs touched
- `docs/progress.md` — Marked 4.1 `done`, set Next task to `4.2`, updated On disk table.
