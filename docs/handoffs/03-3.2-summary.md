# Summary — 3.2 Network rate math with counter-reset handling

## Done
- Enhanced and expanded rate math property testing in `Tests/iStatsCoreTests/RateMathTests.swift`:
  - `testBytesPerSecondZeroElapsedReturnsZero`: Tested zero and negative elapsed time intervals returning 0 B/s.
  - `testBytesPerSecondNeverNegativeProperty`: Validated non-negativity and finiteness across cross-product of counter magnitudes and elapsed times.
  - `testBytesPerSecondCounterResetProperty`: Validated that counter resets (`current < previous`) strictly return `0.0` B/s without negative numbers or false spikes.
  - `testBytesPerSecondScaleInvarianceProperty`: Proved scale invariance `Rate(k * delta, k * t) == Rate(delta, t)`.
  - `testBytesPerSecondMonotonicityWithRespectToCurrentProperty`: Proved monotonicity with respect to increasing counter delta.
  - `testBytesPerSecondMonotonicityWithRespectToElapsedProperty`: Proved strict decrease with increasing elapsed time.
  - `testBytesPerSecondFuzzRandomizedVectors`: Fuzz tested 1,000 pseudo-random test vectors against exact mathematical invariants.
  - `testCounterDeltaMonotonicityProperty` & `testCounterDeltaExtremeValuesNearUInt64Max`: Validated wrapping behavior across 32-bit and 64-bit boundaries.
- Enhanced property and invariant tests in `Tests/iStatsTests/NetworkSamplerTests.swift`:
  - `testSessionTotalsMonotonicityUnderRandomCounterResets`: Executed 100 sequential sampling steps with pseudo-random counter churn and periodic resets, verifying that per-interface and aggregate session totals are strictly monotonic non-decreasing while instantaneous rates remain well-formed (0 B/s on reset).
  - `testZeroOrNegativeTimeStepYieldsZeroRate`: Tested clock stability invariants (0 B/s on non-positive time intervals).
  - `testExtremeCountersNearUInt64Max`: Verified rate and session accumulation near `UInt64.max` and overflow wraps.
- Verified test suites:
  - `swift test --scratch-path /tmp/istats-build` — 91 passed (0 failures).
  - `xcodebuild test -scheme iStatsApp -destination 'platform=macOS'` — 46 passed (0 failures).
  - `xcodebuild -scheme iStats -configuration Debug build` — BUILD SUCCEEDED.

## Files changed
- `Tests/iStatsCoreTests/RateMathTests.swift` — Added property tests for `RateMath.bytesPerSecond` and `RateMath.counterDelta`
- `Tests/iStatsTests/NetworkSamplerTests.swift` — Added session total monotonicity and clock invariant property tests
- `docs/handoffs/03-3.2-plan.md` — Handoff plan
- `docs/handoffs/03-3.2-summary.md` — Handoff summary
- `docs/progress.md` — Updated status and on-disk tracking

## Tests run
- `swift test --scratch-path /tmp/istats-build` — **91 passed** (0 failures)
- `xcodebuild test -scheme iStatsApp -destination 'platform=macOS'` — **46 passed** (0 failures)
- `xcodebuild -scheme iStats -configuration Debug build` — **BUILD SUCCEEDED**

## Not done / follow-ups
- Task 3.3: Disk capacity monitoring (`docs/phases/phase-03-network-disk/tasks/3.3-disk-capacity.md`).
- Task 3.4: Disk I/O monitoring.

## Docs touched
- `docs/progress.md` — Marked 3.2 `done`, set Next task to `3.3`.
