# Exam — 2.5 Memory pressure level + UI surfacing

## Snapshot
- `Sources/iStatsCore/Models.swift`: `MemoryPressure` enum (`normal`, `warning`, `critical`) and `MemorySample` exist.
- `iStats/Sampling/MemorySampler.swift`: `MemorySampler`, `MemoryInfoProvider`, and `HostMemoryInfoProvider` exist. `HostMemoryInfoProvider.memoryPressure()` reads `sysctlbyname("kern.memorystatus_vm_pressure_level", ...)`.
- `iStats/UI/DetailPopoverView.swift`: Currently a placeholder foundation shell; needs to surface memory metrics and visual warning/critical indicators for memory pressure.

## Spec coverage
| Requirement / task bullet | Status | Evidence |
|---------------------------|--------|----------|
| 2.2 Memory pressure level derivation | Implemented via sysctl; enhance with DispatchSource listener | `HostMemoryInfoProvider.memoryPressure()` and `MemoryPressureMonitor` |
| 2.4 Visible UI surfacing of warning/critical pressure | Missing UI representation | Task 2.5 requires prominent UI warning/critical surfacing |
| 12.1 Background thread safety | Maintained | Background memory pressure monitoring off the main thread |

## Relevant types and files
- `Sources/iStatsCore/Models.swift` — `MemoryPressure`
- `iStats/Sampling/MemoryPressureMonitor.swift` (to create) — DispatchSource memory pressure event listener
- `iStats/Sampling/MemorySampler.swift` — Memory provider integration
- `iStats/UI/MemoryPressureView.swift` (to create) — SwiftUI memory pressure indicator and alert banner
- `iStats/UI/DetailPopoverView.swift` — Detail popover view with memory section and pressure alerts
- `Tests/iStatsTests/MemoryPressureTests.swift` (to create) — Tests for pressure level mapping and UI state logic

## Risks and unknowns
- `DispatchSourceMemoryPressure` must be activated on a dedicated background dispatch queue.
- UI styling must adhere to macOS Human Interface Guidelines (green for normal, orange/yellow for warning, red for critical).

## Suggested next role
implementor

## Open questions
None.
