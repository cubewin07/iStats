# Summary — 3.3 Disk capacity per volume

## Done
- Created `DiskSampler` in `iStats/Sampling/DiskSampler.swift`:
  - Defined `RawDiskIOCounters` for block storage I/O representation.
  - Defined `DiskInfoProvider` protocol for dependency injection and deterministic testing.
  - Implemented `HostDiskInfoProvider` using Darwin `getfsstat(..., MNT_NOWAIT)` to read `statfs` structures for all mounted filesystems.
  - Resolved volume display names via `URLResourceKey.volumeLocalizedNameKey` / `volumeNameKey` and `FileManager.default.displayName`.
  - Filtered out 0-byte pseudo/virtual mounts and computed total, used, and free capacity bytes.
  - Implemented `DiskSampler` conforming to `Sampler` (`Output = DiskSample`, `category = .disk`).
  - Background sampling off main thread under `SampleScheduler`.
- Updated `MetricsCoordinator.swift` in `iStats/App/`:
  - Added `@Published public private(set) var latestDisk: Sample<DiskSample>?`.
  - Added `@Published public private(set) var diskHistory: [Sample<DiskSample>] = []`.
  - Registered `DiskSampler()` in `start()`.
  - Handled `.disk` readings in `handleReading(_:)`.
- Updated Xcode Project (`iStats.xcodeproj/project.pbxproj`):
  - Registered `DiskSampler.swift` under `Sampling` group in `iStats` app target.
  - Registered `DiskSamplerTests.swift` under `Tests/iStatsTests` in `iStatsTests` test target.
- Added comprehensive unit tests in `Tests/iStatsTests/DiskSamplerTests.swift`:
  - Category verification (`.disk`).
  - Mounted volume capacity reporting for single and multiple volumes.
  - Dynamic volume addition and removal across sample calls.
  - Sampler error isolation test.
  - Live host provider test on macOS.
- Verified test suites:
  - `swift test --scratch-path /tmp/istats-build` — 91 passed (0 failures).
  - `xcodebuild test -scheme iStatsApp -destination 'platform=macOS'` — 51 passed (0 failures).
  - `xcodebuild -scheme iStats -configuration Debug build` — BUILD SUCCEEDED.

## Files changed
- `iStats/Sampling/DiskSampler.swift` — `DiskSampler`, `RawDiskIOCounters`, `DiskInfoProvider`, `HostDiskInfoProvider`
- `iStats/App/MetricsCoordinator.swift` — Registered `DiskSampler` and published disk state
- `Tests/iStatsTests/DiskSamplerTests.swift` — Unit tests for `DiskSampler`
- `iStats.xcodeproj/project.pbxproj` — Added `DiskSampler.swift` and `DiskSamplerTests.swift`
- `docs/handoffs/03-3.3-plan.md` — Handoff plan
- `docs/handoffs/03-3.3-summary.md` — Handoff summary
- `docs/progress.md` — Updated status and on-disk tracking

## Tests run
- `swift test --scratch-path /tmp/istats-build` — **91 passed** (0 failures)
- `xcodebuild test -scheme iStatsApp -destination 'platform=macOS'` — **51 passed** (0 failures)
- `xcodebuild -scheme iStats -configuration Debug build` — **BUILD SUCCEEDED**

## Not done / follow-ups
- Task 3.4: Disk I/O throughput via IOKit block storage statistics (`IOBlockStorageDriver`).

## Docs touched
- `docs/progress.md` — Marked 3.3 `done`, set Next task to `3.4`, updated On disk table.
