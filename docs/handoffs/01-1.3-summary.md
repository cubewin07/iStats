# Summary — 1.3 Define core protocols and value types

## Done
- Enhanced `Availability.swift`:
  - Added `Codable` conformance and `unavailableReason: String?` helper property to `Availability`.
  - Added default `timestamp: Date = Date()` to `Sample.init`.
  - Added conditional `Equatable` and `Codable` conformances to `Sample<Value>`.
- Enhanced `MetricCategory.swift`:
  - Added `Identifiable` conformance (`id: String { rawValue }`).
- Expanded `Models.swift`:
  - Added `ThermalPressure` (`nominal`, `fair`, `serious`, `critical`).
  - Added `ThermalSample` composite wrapper.
  - Added `FanSample` composite wrapper.
  - Added `NetworkSample` composite wrapper with aggregate throughput helpers (`totalBytesInPerSec`, `totalBytesOutPerSec`, `totalBytesIn`, `totalBytesOut`).
  - Added `VolumeCapacity`, `DiskIO`, and `DiskSample` composite wrapper.
  - Added `BatteryState` and `PowerSample` composite wrapper.
  - Added `GPUSample` wrapper.
  - Added `Codable` conformances across all domain value types.
- Created comprehensive unit test suites in `Tests/iStatsCoreTests/`:
  - `AvailabilityTests.swift`: tests availability states, error reasons, equatability, and sample wrapping/coding.
  - `MetricCategoryTests.swift`: tests category coverage, display names, identifiable id, and coding.
  - `SamplerTests.swift`: tests `Sampler` protocol conformance with mock samplers, throwing sampler errors, and `SamplerError` equality.
  - `ModelsTests.swift`: tests instantiation, properties, equatability, and aggregate calculations for all models.
- Verified test suite: all 44 unit tests pass (`swift test --scratch-path /tmp/istats-build`).
- Verified app target build: `xcodebuild -scheme iStatsApp -derivedDataPath ./build build` succeeded.

## Files changed
- `Sources/iStatsCore/Availability.swift` — Added Codable, Equatable conformances and helper accessors
- `Sources/iStatsCore/MetricCategory.swift` — Added Identifiable conformance
- `Sources/iStatsCore/Models.swift` — Added composite metric types for Thermal, Fan, Network, Disk, Power, and GPU
- `Tests/iStatsCoreTests/AvailabilityTests.swift` — Unit tests for Availability and Sample
- `Tests/iStatsCoreTests/MetricCategoryTests.swift` — Unit tests for MetricCategory
- `Tests/iStatsCoreTests/SamplerTests.swift` — Unit tests for Sampler protocol and SamplerError
- `Tests/iStatsCoreTests/ModelsTests.swift` — Unit tests for all pure model types
- `docs/handoffs/01-1.3-exam.md` — Examiner report
- `docs/handoffs/01-1.3-plan.md` — Implementation plan
- `docs/handoffs/01-1.3-summary.md` — Implementation summary

## Tests run
- `swift test --scratch-path /tmp/istats-build` — **44 passed**
- `xcodebuild -scheme iStatsApp -derivedDataPath ./build build` — **BUILD SUCCEEDED**

## Not done / follow-ups
- Task 1.4: Implement `SampleScheduler`.
- Task 1.5: Implement `MetricsStore`.

## Docs touched
- `docs/progress.md` — Marked 1.3 `done`, set Next task to `1.4`, updated On disk table.
