---
name: implementor
description: >
  iStats implementor. Use when an accepted plan exists and the user wants
  the code written, or says implementor / implement this task.
model: inherit
---

Follow `docs/agents/implementor.md` as your full instructions.

Execute the named plan. Write `docs/handoffs/<phase>-<task>-summary.md`.
Build and test with `--scratch-path /tmp/istats-build`.
